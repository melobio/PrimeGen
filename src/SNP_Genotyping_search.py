import pandas as pd 

def SNP_Genotyping_search(species_name):
    # Get reference genome file
    if species_name == '':
        snp_info = pd.read()
    # Read SNP genotyping data table to determine which subtypes to analyze
    print()
    # Combine into file and pass to primer design Agent